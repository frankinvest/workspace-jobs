# OpenClaw 强制开发与版本控制规范 (GitOps)

## 1. 绝对禁止直推主干
严禁任何情况下直接对 `main` 或 `master` 分支执行代码修改和 `git push`。

## 2. 强制分支工作流 (Branching Workflow)
每次接收到新的修改指令，必须按照以下步骤执行：
- 基于当前的 `main` 分支，检出一个新的功能分支（格式：`feat/任务简述` 或 `fix/bug简述`，附带日期，例如 `fix/cdp-scroll-0528`）。
- 在新分支上进行所有代码修改、测试。
- 修改完成后，执行完整的 commit。

## 3. 强制版本标签 (Tagging)
当在分支上完成阶段性修改（即使是部分完成功能）并准备向我汇报前，必须为该次提交打上版本标签（Tag）。
- 版本号规范：`v大版本.功能进度.修复次数`（例如 `v1.2.1`）。
- 必须包含清晰的注释：`git tag -a v1.2.1 -m "完成 60% 抓取逻辑，图片暂报 403"`。

## 4. 提交与 Vercel 预览审查 (Review Process)
将分支和 Tag 推送到远程仓库：`git push origin <分支名>` 和 `git push --tags`。
推送完成后，【必须停止动作，等待人类审查】。等待 Vercel 针对该分支生成的预览链接（Preview URL）。

## 5. 标准化战报格式
向人类汇报时，只允许使用以下精简格式，严禁啰嗦：
- **当前分支**: `[分支名]`
- **版本号 (Tag)**: `[vX.X.X]`
- **变更摘要**: `[简短说明你改了什么文件，修了什么问题]`
- **待审查点**: `[Vercel Preview URL 或本地审查建议]`
